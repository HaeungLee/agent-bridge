# 20260528 Process

## 20:?? - Codex - Benchmark evidence format and seed records

- **Status**: completed
- **Summary**: Added a tracked benchmark evidence format and seeded it with commander-curated records from valid Aider, OpenCode, Antigravity, and Claude proxy runs. Updated model observations to summarize routing candidates by task class instead of treating models as globally ranked.
- **Files Changed**:
  - `docs/benchmarks/README.md`
  - `docs/benchmarks/model_benchmarks.jsonl`
  - `docs/model_observations.md`
  - `docs/process/20260528_process.md`
- **Commands Run**:
  - `Get-Content -Raw docs/plan/agent_bridge_mvp.md`
  - `Get-Content -Raw docs/model_observations.md`
  - `Get-Content -Raw src/agent_bridge/evaluator.py`
  - `Get-ChildItem -Path .agent\runs -Directory`
  - Read selected `metrics.json` and `touched_files.json` artifacts for curated benchmark runs.
  - `python` JSONL parse check for `docs/benchmarks/model_benchmarks.jsonl`
- **Result**:
  - Added `benchmark_evidence.v0` JSONL records for 9 real harness/model runs.
  - Excluded mock, placeholder, blocked, lost transient, and no-op restored-history runs from candidate evidence.
  - Kept caveated real runs as `qualified_observation` instead of promoting them to default routing candidates.
  - Documented task-class candidates for `readonly_report`, `worktree_patch`, `worktree_patch_continue`, and `deep_review_or_investigation`.
  - Verified `docs/benchmarks/model_benchmarks.jsonl` parses as 9 valid JSONL records.
- **Risks**:
  - Records are manually curated for now; future CLI support should reduce copy/paste drift from run artifacts.
  - Some gate statuses are historical notes rather than freshly re-run gates.
- **Open Questions**:
  - Whether to add `agent-bridge bench record` for future evidence capture.
  - Whether to generate a task-class routing summary from `model_benchmarks.jsonl`.
- **Commander Verdict**: N/A

## 14:40 - Codex - Benchmark record CLI

- **Status**: completed
- **Summary**: Added `agent-bridge bench record` so future commander-curated benchmark evidence can be appended from run artifacts without manually copying core metadata. The command keeps commander judgment fields explicit while deriving agent, runner, provider, model, runtime, touched file count, final report presence, and task ID from `.agent/runs/<run_id>/`.
- **Files Changed**:
  - `src/agent_bridge/benchmarks.py`
  - `src/agent_bridge/cli.py`
  - `test/test_benchmarks.py`
  - `docs/benchmarks/README.md`
  - `docs/process/20260528_process.md`
- **Commands Run**:
  - `uv run pytest test/test_benchmarks.py test/test_compare.py test/test_process_rollup.py`
  - `uv run python -m compileall src`
  - `uv run agent-bridge bench record --run 20260524-192821-a39435-aider_deepseek_flash --kind worktree_patch --status accepted_candidate --harness aider --gate passed --instruction good --scope write_scope_only --best-use "default cheap fast worktree_patch candidate for small scoped changes" --avoid "large ambiguous refactors until broader evidence exists" --notes "CLI smoke record into temp JSONL." --out .agent/reports/bench_record_smoke.jsonl --allow-duplicate`
- **Result**:
  - Added `benchmark_evidence.v0` record construction from normalized run artifacts.
  - Added append-only JSONL writing with duplicate `run_id` + `run_kind` protection.
  - Added CLI parser and output for `agent-bridge bench record`.
  - Added tests for record construction, duplicate rejection, and CLI append behavior.
  - CLI smoke succeeded and wrote a temporary JSONL under `.agent/reports/`, then the temporary smoke file was removed.
- **Risks**:
  - `bench record` still depends on commander-supplied qualitative fields such as instruction following and scope discipline.
  - It does not run gates itself; `--gate` records the commander-observed gate status.
- **Open Questions**:
  - Whether to add `agent-bridge bench summarize` to aggregate task-class candidates from `model_benchmarks.jsonl`.
- **Commander Verdict**: N/A
