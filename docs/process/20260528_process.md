# 20260528 Process

## 20:?? - Codex - Benchmark evidence format and seed records

- **Status**: completed
- **Summary**: Added a tracked benchmark evidence format and seeded it with commander-curated records from valid Aider, OpenCode, Antigravity, and Claude proxy runs. Updated model observations to summarize routing candidates by task class instead of treating models as globally ranked.
- **Files Changed**:
  - `docs/benchmarks/README.md`
  - `docs/benchmarks/model_benchmarks.jsonl`
  - `docs/model_observations.md`
  - `docs/process/20260528_process.md`
- **Commands Run**:
  - `Get-Content -Raw docs/plan/agent_bridge_mvp.md`
  - `Get-Content -Raw docs/model_observations.md`
  - `Get-Content -Raw src/agent_bridge/evaluator.py`
  - `Get-ChildItem -Path .agent\runs -Directory`
  - Read selected `metrics.json` and `touched_files.json` artifacts for curated benchmark runs.
  - `python` JSONL parse check for `docs/benchmarks/model_benchmarks.jsonl`
- **Result**:
  - Added `benchmark_evidence.v0` JSONL records for 9 real harness/model runs.
  - Excluded mock, placeholder, blocked, lost transient, and no-op restored-history runs from candidate evidence.
  - Kept caveated real runs as `qualified_observation` instead of promoting them to default routing candidates.
  - Documented task-class candidates for `readonly_report`, `worktree_patch`, `worktree_patch_continue`, and `deep_review_or_investigation`.
  - Verified `docs/benchmarks/model_benchmarks.jsonl` parses as 9 valid JSONL records.
- **Risks**:
  - Records are manually curated for now; future CLI support should reduce copy/paste drift from run artifacts.
  - Some gate statuses are historical notes rather than freshly re-run gates.
- **Open Questions**:
  - Whether to add `agent-bridge bench record` for future evidence capture.
  - Whether to generate a task-class routing summary from `model_benchmarks.jsonl`.
- **Commander Verdict**: N/A

## 14:40 - Codex - Benchmark record CLI

- **Status**: completed
- **Summary**: Added `agent-bridge bench record` so future commander-curated benchmark evidence can be appended from run artifacts without manually copying core metadata. The command keeps commander judgment fields explicit while deriving agent, runner, provider, model, runtime, touched file count, final report presence, and task ID from `.agent/runs/<run_id>/`.
- **Files Changed**:
  - `src/agent_bridge/benchmarks.py`
  - `src/agent_bridge/cli.py`
  - `test/test_benchmarks.py`
  - `docs/benchmarks/README.md`
  - `docs/process/20260528_process.md`
- **Commands Run**:
  - `uv run pytest test/test_benchmarks.py test/test_compare.py test/test_process_rollup.py`
  - `uv run python -m compileall src`
  - `uv run agent-bridge bench record --run 20260524-192821-a39435-aider_deepseek_flash --kind worktree_patch --status accepted_candidate --harness aider --gate passed --instruction good --scope write_scope_only --best-use "default cheap fast worktree_patch candidate for small scoped changes" --avoid "large ambiguous refactors until broader evidence exists" --notes "CLI smoke record into temp JSONL." --out .agent/reports/bench_record_smoke.jsonl --allow-duplicate`
- **Result**:
  - Added `benchmark_evidence.v0` record construction from normalized run artifacts.
  - Added append-only JSONL writing with duplicate `run_id` + `run_kind` protection.
  - Added CLI parser and output for `agent-bridge bench record`.
  - Added tests for record construction, duplicate rejection, and CLI append behavior.
  - CLI smoke succeeded and wrote a temporary JSONL under `.agent/reports/`, then the temporary smoke file was removed.
- **Risks**:
  - `bench record` still depends on commander-supplied qualitative fields such as instruction following and scope discipline.
  - It does not run gates itself; `--gate` records the commander-observed gate status.
- **Open Questions**:
  - Whether to add `agent-bridge bench summarize` to aggregate task-class candidates from `model_benchmarks.jsonl`.
- **Commander Verdict**: N/A

## 15:27 - Codex - Small worktree_patch benchmark expansion

- **Status**: completed
- **Summary**: Added and executed two small worktree_patch benchmark tasks across Aider DeepSeek Flash, Aider Mimo, and OpenCode Kimi. One task modified an existing function, and one task added focused tests. Recorded all six runs into `docs/benchmarks/model_benchmarks.jsonl` with task-class-specific evidence statuses.
- **Files Changed**:
  - `.agent/tasks/bench_worktree_patch_parse_failures.toml`
  - `.agent/tasks/bench_worktree_patch_parse_failures.md`
  - `.agent/tasks/bench_worktree_patch_benchmark_path_tests.toml`
  - `.agent/tasks/bench_worktree_patch_benchmark_path_tests.md`
  - `docs/benchmarks/model_benchmarks.jsonl`
  - `docs/model_observations.md`
  - `docs/process/20260528_process.md`
- **Commands Run**:
  - `uv run agent-bridge task validate --spec .agent/tasks/bench_worktree_patch_parse_failures.toml`
  - `uv run agent-bridge task render --spec .agent/tasks/bench_worktree_patch_parse_failures.toml --out .agent/tasks/bench_worktree_patch_parse_failures.md`
  - `uv run agent-bridge task validate --spec .agent/tasks/bench_worktree_patch_benchmark_path_tests.toml`
  - `uv run agent-bridge task render --spec .agent/tasks/bench_worktree_patch_benchmark_path_tests.toml --out .agent/tasks/bench_worktree_patch_benchmark_path_tests.md`
  - `uv run agent-bridge run --agent aider_deepseek_flash --task .agent/tasks/bench_worktree_patch_parse_failures.md --workspace .` (Run ID: `20260528-151613-a05eef-aider_deepseek_flash`)
  - `uv run agent-bridge run --agent aider_mimo_pro --task .agent/tasks/bench_worktree_patch_parse_failures.md --workspace .` (Run ID: `20260528-151647-aab309-aider_mimo_pro`)
  - `uv run agent-bridge run --agent opencode_kimi_impl --task .agent/tasks/bench_worktree_patch_parse_failures.md --workspace .` (Run ID: `20260528-151751-bb58a9-opencode_kimi_impl`)
  - `uv run agent-bridge run --agent aider_deepseek_flash --task .agent/tasks/bench_worktree_patch_benchmark_path_tests.md --workspace .` (Run ID: `20260528-151949-d6d5e1-aider_deepseek_flash`)
  - `uv run agent-bridge run --agent aider_mimo_pro --task .agent/tasks/bench_worktree_patch_benchmark_path_tests.md --workspace .` (Run ID: `20260528-152038-ee0c0f-aider_mimo_pro`)
  - `uv run agent-bridge run --agent opencode_kimi_impl --task .agent/tasks/bench_worktree_patch_benchmark_path_tests.md --workspace .` (Run ID: `20260528-152214-1e9f69-opencode_kimi_impl`)
  - `uv run agent-bridge task gate --spec <spec> --run <run-id> --workspace .` for all six runs
  - Applied selected patches to temporary verification worktrees and ran inline assertions or `pytest test/test_benchmarks.py`
  - `uv run agent-bridge bench record ...` for all six benchmark runs
- **Result**:
  - Existing-function edit sample:
    - Aider DeepSeek Flash: 25.11s, no patch, gate rejected.
    - Aider Mimo: 57.14s, no patch, gate rejected.
    - OpenCode Kimi: 81.21s, correct patch; inline assertions passed in a temporary verification worktree. The task gate still rejected due absolute worktree tool-use path normalization.
  - Test-addition sample:
    - Aider DeepSeek Flash: 42.10s, patch produced and gate passed, but isolated pytest failed on Windows path semantics.
    - Aider Mimo: 87.74s, no patch, gate rejected.
    - OpenCode Kimi: 60.11s, patch produced; isolated pytest passed. The task gate still rejected due absolute worktree tool-use path normalization.
  - Updated `docs/model_observations.md` so worktree_patch routing is task-class-specific instead of treating the earlier new-file benchmark as sufficient for all patch work.
- **Risks**:
  - OpenCode Kimi quality is currently understated by task gate because tool-use path normalization treats absolute isolated worktree paths as outside the commander workspace.
  - Aider direct adapter may need explicit `--file`/`--read` arguments or a stronger edit-forcing prompt for existing-file edits.
- **Open Questions**:
  - Should `task gate` normalize tool-use paths against `worktree.json.worktree_path` for worktree_patch runs?
  - Should the Aider adapter pass write_scope files as explicit `--file` arguments?
- **Commander Verdict**: N/A

## 19:34 - Codex - worktree_patch tool-use path normalization

- **Status**: completed
- **Summary**: Updated the task gate so `worktree_patch` runs normalize tool-use absolute paths against `worktree.json.worktree_path` instead of the commander workspace. Re-ran the two OpenCode Kimi benchmark gates that previously failed on absolute isolated-worktree paths; both now pass.
- **Files Changed**:
  - `src/agent_bridge/task_spec.py`
  - `test/test_task_patch_gate.py`
  - `docs/benchmarks/model_benchmarks.jsonl`
  - `docs/process/20260528_process.md`
- **Commands Run**:
  - `uv run pytest test/test_task_patch_gate.py`
  - `uv run python -m compileall src`
  - `uv run agent-bridge task gate --spec .agent/tasks/bench_worktree_patch_parse_failures.toml --run 20260528-151751-bb58a9-opencode_kimi_impl --workspace .`
  - `uv run agent-bridge task gate --spec .agent/tasks/bench_worktree_patch_benchmark_path_tests.toml --run 20260528-152214-1e9f69-opencode_kimi_impl --workspace .`
  - `uv run pytest test/test_task_patch_gate.py test/test_benchmarks.py test/test_compare.py`
- **Result**:
  - Added regression coverage for accepting absolute paths inside the isolated worktree.
  - Added regression coverage for still rejecting absolute paths outside the isolated worktree.
  - Existing-function OpenCode Kimi run now passes gate with 10 tool uses and 1 changed file.
  - Test-addition OpenCode Kimi run now passes gate with 6 tool uses and 1 changed file.
  - Updated benchmark evidence records from `tool_use_path_false_positive_patch_verified` to `passed_after_tool_path_normalization`.
- **Risks**:
  - If a malformed or missing `worktree.json` is present, tool-use normalization falls back to the commander workspace; the artifact gate and patch metadata checks still report invalid worktree metadata separately.
  - Gate strictness remains scope-focused; it does not replace commander or reviewer validation of semantic correctness.
- **Open Questions**:
  - Whether to add a distinct warning channel later for reviewable, non-fatal observations, while keeping scope violations and empty patches as hard failures.
- **Commander Verdict**: N/A
